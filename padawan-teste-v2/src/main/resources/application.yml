spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/padawan
    username: postgres
    password: Kamilly20
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 9099
